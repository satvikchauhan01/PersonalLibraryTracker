REACT_APP_API_URL=http://localhost:5001/api

# Phase 17: Sentry (Settings → Client Keys (DSN) on a React project) — baked
# into the build at build time like every other REACT_APP_* var.
REACT_APP_SENTRY_DSN=
